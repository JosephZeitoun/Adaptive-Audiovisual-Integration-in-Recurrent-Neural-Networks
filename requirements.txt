# Adaptive Audiovisual Integration in Recurrent Neural Networks
# Python 3.10+.  CPU only — no GPU is required.
#     pip install -r requirements.txt

# core
torch>=2.0,<3.0            # GRU, training loop, autograd Jacobians
numpy>=1.24,<3.0
scipy>=1.10,<2.0           # scipy.optimize.minimize (L-BFGS-B) for fixed points
scikit-learn>=1.3,<2.0     # LogisticRegression, cross_val_score, GroupKFold
matplotlib>=3.7,<4.0

# notebook environment
jupyterlab>=4.0
ipykernel>=6.25

# optional: notebook 13 renders one interactive 3-D view with plotly.
# The cell is wrapped in try/except and is skipped if plotly is absent,
# so nothing else depends on it.
plotly>=5.15
